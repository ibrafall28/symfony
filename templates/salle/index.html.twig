{% extends 'accueil.html.twig' %}

{% block title %}Hello SalleController!{% endblock %}

{% block body %}
<div class="row">
    <div class="container col-md-11">
        <div class="card">
        {% for message in app.flashes('success') %}
            <div class ="alert alert-success">
            {{ message }}
            </div>
            {% endfor %}
            <div class="card-header">Gestion de la liste des Salles</div>
            <div class="card-body">
                <table class="table table-striped">
                    <tr>
                        <th>Identifiant</th>
                        <th>nom</th>
                        <th>nom user</th>
                         <th>Action</th>
                          <th>Action</th>
                    </tr>
                    {% for s in salles %}
                        <tr>
                            <td>{{ s.id }}</td>
                            <td>{{ s.nom }}</td>
                            <td>{{ s.users }}</td>
                           users
                             <td><a href="{{ path('edit',{ 'id': s.id }) }}">Edite<a/></td>
                            <td><a href="{{ path('delate',{ 'id': s.id }) }}">Spprimer<a/></td>
                            <td><a href="{{ path('liste') }}">Imprimer<a/></td>
                        </tr>
                     {%endfor%}
                </table>
            
</div>
        </div>
                    
    </div>
                     
    <div class="container col-md-9">
        <div class="card">
            <div class="card-header">Forùulaire  des Salles</div>
            <div class="card-body">
          {{ form(form )}}
            </div>
        </div>
    </div>
    </div>
{% endblock %}
